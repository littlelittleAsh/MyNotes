1、在 C++ 中，如果你在类的外部定义成员函数（而不是在类的声明里定义），需要通过 类名 + “ :: ”，其中，
  内部定义为：
class Dialog {
public:
    Dialog(QWidget *parent); // 构造函数的声明
};
  外部定义为：
Dialog::Dialog(QWidget *parent) {
    // 定义构造函数
}
2、内联函数加 inline 即可，另外，类定义中定义的函数都是内联函数；
3、初始化列表，大部分都是成员变量，还有一部分是基类的构造函数；


    
