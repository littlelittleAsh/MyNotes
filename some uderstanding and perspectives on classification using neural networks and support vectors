    今天新学习了支持向量机（SVM）的相关概念，为了加深理解我将它和卷积神经网络（CNN）稍加对比。
    由于两种算法的侧重方向不同，甚至差异较大，我只在分类问题上进行对比，记录我的观点。（由于刚刚入门，了解很浅，仅仅只是记录自己的理解和一些看法）
在分类问题上：
    1、CNN的本质是在描边。通过卷积核来不断突出边缘特征，以实现，，额，，某种目的
    2、SVM的本质是通过不断扭曲、拉伸原始数据所在空间平面，使得可以通过一个平面将拉伸、扭曲后的空间平面一分为二，此时得到的两个平面的交线即为分类器。
